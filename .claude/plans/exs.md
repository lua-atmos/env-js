# Plan: exs

Add sample files and reorganize README sections.

## Tasks

### 1. Create sample files in `exs/`

- [x] `exs/hello.lua` — bare Lua hello world (`print("hello")`)
- [ ] `exs/hello-atmos.lua` — lua-atmos mode: watch 5s,
  print "hello" every second
- [ ] `exs/hello.atm` — Atmos compiler mode: same behavior

### 2. Reorganize README.md

- [ ] Rename "## Usage" + "## Generating a hash" → "## Remote Usage"
- [ ] Rename "## Running a file" → "## Local Usage"
- [ ] Reorder: Local Usage before Remote Usage
- [ ] Update examples to use `exs/` paths
- [ ] Keep "## HTML files" at top, "## Rebuilding" at bottom

## Progress

- (none yet)
